appId: 'yudicolauncher'
productName: 'Yudico Launcher'
artifactName: '${productName}-setup-${version}.${ext}'

copyright: 'Copyright © 2026 Mayco_Veio'

asar: true
compression: 'maximum'

files:
  - '!{dist,.gitignore,.vscode,docs,dev-app-update.yml,.nvmrc,eslint.config.mjs}'

extraResources:
  - 'libraries'

# Windows Configuration
win: 
  target: 
    - target: 'nsis'
      arch: 'x64'

# Windows Installer Configuration
nsis:
  oneClick: false
  perMachine: false
  allowElevation: true
  allowToChangeInstallationDirectory: true

# macOS Configuration
mac:
  target:
    - target: 'dmg'
      arch:
        - 'x64'
        - 'arm64'
  artifactName: '${productName}-setup-${version}-${arch}.${ext}'
  category: 'public.app-category.games'

# Linux Configuration
linux: 
  target: 'AppImage'
  maintainer: 'Mayco_Veio'
  vendor: 'Mayco_Veio'
  synopsis: 'Moderador Minecraft Yudico Launcher'
  description: 'Um launcher personalizado que permite aos usuários entrarem em servidores com mods. Todos os mods, configurações e atualizações são gerenciados automaticamente.'
  category: 'Game'


directories:
  buildResources: 'build'
  output: 'dist'